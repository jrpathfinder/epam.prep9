package com.epam.learn.springdata.model;

/**
 * The above code defines an enumeration class named "TicketCategory" that contains three constants: STANDARD, PREMIUM, and BAR. This enum can be used to represent different categories of tickets in a ticketing system.
 * The STANDARD category could represent regular tickets, PREMIUM could represent VIP tickets, and BAR could represent tickets for access to a bar or lounge area. This enum provides a convenient and type-safe way to work with these categories throughout the codebase.
 */
public enum TicketCategory {
    STANDARD,
    PREMIUM,
    BAR
}
